---
layout: layout.njk
---
{% starterMessage %}
<div class="starter-message max-w-xl">
	<h3 class="text-2xl lg:text-4xl">Quick Guide to Speedlify</h3>
	<ol class="mt-4 text-gray-500 font-medium">
		<li>Add your own URLs to test in a <code>_data/sites/*.js</code> configuration file. Look at the existing samples (Make sure you use <code>skip: false</code>). You can delete the existing ones after you’ve made your own</li>
		<li>Remove this message from <code>index.njk</code></li>
	</ol>
</div>
{% endstarterMessage %}
<table class="leaderboard leaderboard-list">
	<thead data-sr-only>
		<tr>
			<th>Category</th>
			<th>Site Count</th>
			<th>Last Run Date</th>
		</tr>
	</thead>
	<tbody>
	{%- for key, category in sites %}
		{%- if not category.hide %}
		<div class=" mt-12 pt-12 border-t">
		<div class="py-2 px-4 bg-gray-50 flex justify-between items-center rounded-lg">
				<a class="text-xl text-gray-500" href="{{ ('/' + (category.name or key) | lower | slug + '/') | url }}">{{ category.description or category.name or key }}</a>
				<div>
					<span class="count">×{{ category.urls.length }}<span class="leaderboard-hide-sm"> site{% if category.urls.length != 1 %}s{% endif %}</span></span>
					<span class="leaderboard-hide-sm">{% if lastruns[key].timestamp %}<span class="small"><timestamp-ago timestamp="{{ lastruns[key].timestamp }}">{{ lastruns[key].timestamp | displayDate | safe }}</timestamp-ago></span>{% endif %}</span>
				</div>
		</div>
		</div>
		{%- endif %}
	{%- endfor %}
	</tbody>
</table>
